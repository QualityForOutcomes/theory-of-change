# Stripe Configuration
# Get your keys from: https://dashboard.stripe.com/apikeys
STRIPE_SECRET_KEY=sk_test_your_stripe_secret_key_here

# Server Configuration
PORT=3001
NODE_ENV=development

# CORS Configuration (if needed)
ALLOWED_ORIGINS=http://localhost:3001,http://localhost:3002

# Optional: Free tier price ID (used to detect $0 subscriptions)
STRIPE_FREE_PRICE_ID=price_free_tier_example
 
# Optional: Force frontend origin for success/cancel URL normalization
# FRONTEND_ORIGIN will be preferred when success_url is missing
FRONTEND_ORIGIN=http://localhost:3000